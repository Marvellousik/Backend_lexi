services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: zuri-postgres
    environment:
      POSTGRES_USER: zuri
      POSTGRES_PASSWORD: zuri_secret
      POSTGRES_DB: zuri
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U zuri"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - zuri-network

  # Redis Cache
  redis:
    image: redis:7-alpine
    container_name: zuri-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - zuri-network

  # MinIO Object Storage
  minio:
    image: minio/minio:latest
    container_name: zuri-minio
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin_secret
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3
    networks:
      - zuri-network

volumes:
  postgres_data:
  redis_data:
  minio_data:

networks:
  zuri-network:
    driver: bridge
